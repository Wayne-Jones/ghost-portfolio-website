## ADDED Requirements

### Requirement: About Page Content
The system SHALL provide a static "About" page at the route `/about` that displays information about the site owner or project.

#### Scenario: Render About page
- **WHEN** a user navigates to `/about`
- **THEN** the server renders a page containing a heading "About" and descriptive text about the creator, with proper SEO title metadata.
